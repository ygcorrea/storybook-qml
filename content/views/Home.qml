import QtQuick
import QtQuick.Controls
import QtQuick.Controls.Basic

Rectangle {
    width: parent.width
    height: parent.height
    color: "lightblue"

    Text {
        text: "Home View"
        anchors.centerIn: parent
    }
}
